#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: the character
 * Return: 1 if character is uppercase, 0 if otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
